#!/usr/bin/python3
"""Define a class call Square."""
class Square:
    """Rep a square"""
    def __init__(self, size):
        """Initialize a new Square  instance
        Args:
            size (int): the size
        """
        self.__size = size
